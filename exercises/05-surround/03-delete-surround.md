# Delete Surround

## PRACTICE

```js
const name = ("Steve");
return (result);
const value = (((42)));
let msg = ("hello");
const arr = [[1, 2, 3]];
if ((condition)) {
  run(("command"));
}
const redundant = {"key"};
```

## EXPECTED

```js
const name = "Steve";
return result;
const value = (42);
let msg = "hello";
const arr = [1, 2, 3];
if (condition) {
  run("command");
}
const redundant = "key";
```
