# Append to Register

## PRACTICE

```rust
Collect every TODO comment from the code below into register A
(uppercase = append). Then paste the collected list at the
PASTE HERE marker so all TODOs appear there in order.

fn parse() {
    // TODO: handle empty input
    let x = parse_int();
    // TODO: validate range
    let y = parse_str();
    // TODO: emit warning on overflow
    process(x, y);
}

PASTE HERE:
```

## EXPECTED

```rust
Collect every TODO comment from the code below into register A
(uppercase = append). Then paste the collected list at the
PASTE HERE marker so all TODOs appear there in order.

fn parse() {
    // TODO: handle empty input
    let x = parse_int();
    // TODO: validate range
    let y = parse_str();
    // TODO: emit warning on overflow
    process(x, y);
}

PASTE HERE:
    // TODO: handle empty input
    // TODO: validate range
    // TODO: emit warning on overflow
```
