# Macro with Search

## PRACTICE

Wrap every URL in the markdown below with angle brackets so they
become clickable autolinks: http://example.com → <http://example.com>

Record a macro that finds the next URL with /, then surrounds it.
Replay the macro until every URL is wrapped.

- Docs: http://docs.example.com
- API: http://api.example.com/v1
- Status: http://status.example.com
- Blog: http://blog.example.com/feed

## EXPECTED

Wrap every URL in the markdown below with angle brackets so they
become clickable autolinks: http://example.com → <http://example.com>

Record a macro that finds the next URL with /, then surrounds it.
Replay the macro until every URL is wrapped.

- Docs: <http://docs.example.com>
- API: <http://api.example.com/v1>
- Status: <http://status.example.com>
- Blog: <http://blog.example.com/feed>
